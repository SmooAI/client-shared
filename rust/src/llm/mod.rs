//! # llm — JWT → llm.smoo.ai org-scoped LLM session exchange.
//!
//! **PENDING (pearl th-f7b20f)**. This module is stubbed so the
//! `#[cfg(feature = "llm")] pub mod llm;` declaration in `lib.rs`
//! resolves cleanly — building with `--features llm` today produces
//! no usable surface; landing pearl th-f7b20f fills it in.
